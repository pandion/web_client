define(function () {
	/*
		descriptor: {
			toggle: Boolean,
			close: Boolean,
			position: Boolean,
			events: {
				open: fn(docklet),
				close: fn(docklet),
				show: fn(docklet),
				hide: fn(docklet)
			}
		}
	*/
	return function (descriptor) {
		// TODO: everything.

		var api = {
		};

		return api;
	};
});
